usage:
 
"data".compareTo("data")

This shows the difference between two values.

ex1:
println("A".compareTo("B"))

results in "-1", as "B" is exactly "-1" from "A"
 in alphabetic data terms.

ex2:
println("1".compareTo("5"))

results in "-4", as "5" is "-4" from "1" in
numeric terms.
