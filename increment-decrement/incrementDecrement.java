public class incrementDecrement {
    /** Explains available increment/decrement usage.
     *  The difference between postfix(x++) and prefix(++x) is:
     *
     *  Prefix increments a value, then uses the new
     *  value in expression.
     *
     *  Postfix: the current value is first used in the expression
     *  then it is incremented
  public static void main(String[] args) {
    int x = 2;
    
